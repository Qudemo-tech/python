# Basic requirements for initial deployment (Python 3.13 compatible)
setuptools>=69.0.0
wheel>=0.42.0

# Core dependencies only
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
requests>=2.32.0

# Video processing (essential only)
yt-dlp>=2024.3.10

# Data science (minimal)
numpy>=1.26.0

# Vector DBs (essential only)
supabase>=2.12.0 